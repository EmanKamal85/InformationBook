<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.informationbook">

    <application
        android:allowBackup="true"
        android:icon="@drawable/information_book_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/information_book_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
        <activity android:name=".activities.WondersActivity"></activity>
        <activity android:name=".activities.MuseumsActivity" />
        <activity android:name=".activities.CountriesActivity" />
        <activity android:name=".activities.LeadersActivity" />
        <activity android:name=".activities.MainActivity"
            android:theme="@style/Theme.AppCompat.Light.DarkActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
    </application>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>


</manifest>